<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easywing.platform.system.mapper.SysMenuMapper">

    <resultMap id="SysMenuResult" type="com.easywing.platform.system.domain.entity.SysMenu">
        <id property="id" column="menu_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="queryParam" column="query_param"/>
        <result property="isFrame" column="is_frame"/>
        <result property="isCache" column="is_cache"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="permsType" column="perms_type"/>
        <result property="icon" column="icon"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <select id="selectMenusByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        INNER JOIN sys_role r ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId} AND m.deleted = 0 AND m.status = 0 AND r.status = 0 AND r.deleted = 0
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenusByRoleId" resultMap="SysMenuResult">
        SELECT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.deleted = 0
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectPermsByUserId" resultType="String">
        SELECT DISTINCT m.perms FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        INNER JOIN sys_role r ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId} AND m.deleted = 0 AND m.status = 0
        AND r.status = 0 AND r.deleted = 0 AND m.perms IS NOT NULL AND m.perms != ''
    </select>

    <select id="selectPermsByRoleId" resultType="String">
        SELECT DISTINCT m.perms FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.deleted = 0 AND m.status = 0
        AND m.perms IS NOT NULL AND m.perms != ''
    </select>

    <select id="selectMenuList" resultMap="SysMenuResult">
        SELECT * FROM sys_menu WHERE deleted = 0 ORDER BY parent_id, order_num
    </select>

    <select id="selectChildrenCountByParentId" resultType="int">
        SELECT COUNT(1) FROM sys_menu WHERE parent_id = #{parentId} AND deleted = 0
    </select>

    <select id="checkMenuExistRole" resultType="int">
        SELECT COUNT(1) FROM sys_role_menu WHERE menu_id = #{menuId}
    </select>

</mapper>
